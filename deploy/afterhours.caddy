# Dropped into /opt/marty-media/deploy/conf.d/ on the server. The shared
# Caddy imports conf.d/*.caddy and fetches the certificate on first hit.
after-hour.net {
	encode gzip
	reverse_proxy after-hours-web-1:3000
}

www.after-hour.net {
	redir https://after-hour.net{uri} permanent
}
